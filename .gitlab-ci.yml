image: python:3.11

stages:
  - test
ci_test_v1:
  stage: test
  script:
    - echo "🧪 Styl2 test v1"
ci_test_v2:
  stage: test
  script:
    - echo "🐍 Styl2 test v2"
    - python agent.py
ci_test_v3:
  stage: test
  before_script:
    - pip install python-dotenv
  script:
    - echo "🔐 Test v3: Načítám CI proměnné"
    - python -c "from dotenv import load_dotenv; import os; load_dotenv(); print('CI_TOKEN:', os.getenv('CI_TOKEN'))"
